
randomize();

image_speed = 0;

rotationSpeed = random_range(5,15);
scale = random_range(0.2,1);

grv = 0.2;
initialVsp = -random_range(2.5,5);
vsp = initialVsp;

hsp = random_range(-2,2);

alphaSpeed = 0.03;

sprIndex = irandom_range(0,3);
image_index = sprIndex;

image_xscale = scale;
image_yscale = scale;