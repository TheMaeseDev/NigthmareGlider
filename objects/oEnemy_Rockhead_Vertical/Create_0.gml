
xspd=0;
yspd=0;

restFrames = 90;
restTimer = restFrames;

state = "rest"; //"rest" or "moving"

accel = 0.5;  // Aceleración
maxSpeed = 8; // Velocidad máxima
dirX = 0;     // Dirección en X (-1, 0, 1)
dirY = 1;     // Dirección en Y (-1, 0, 1)