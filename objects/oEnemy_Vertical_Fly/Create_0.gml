event_inherited();

hp=1;
grv = 0;

startY = y;       // Guardamos la posición inicial
rangeY = 30;      // Distancia que subirá y bajará
vspd = 2;        // Velocidad de movimiento
directionY = -1;   // 1 = Subir, -1 = Bajar
timer = 0;

color = c_aqua;