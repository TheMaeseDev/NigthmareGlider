draw_self();
/*
// Variables de posición
var x1 = platA.x + platA.sprite_width / 2;
var y1 = platA.y + platA.sprite_height / 2;
var x2 = platB.x + platB.sprite_width / 2;
var y2 = platB.y + platB.sprite_height / 2;
var thickness = 4; // Grosor de la línea

// Calcular la perpendicular
var angle = point_direction(x1, y1, x2, y2);
var perp_x = lengthdir_x(thickness / 2, angle + 90);
var perp_y = lengthdir_y(thickness / 2, angle + 90);

// Dibujar un rectángulo usando triángulos
draw_set_color(c_black);
draw_primitive_begin(pr_trianglestrip);
draw_vertex(x1 + perp_x, y1 + perp_y);
draw_vertex(x1 - perp_x, y1 - perp_y);
draw_vertex(x2 + perp_x, y2 + perp_y);
draw_vertex(x2 - perp_x, y2 - perp_y);
draw_vertex(x1 - perp_x, y1 - perp_y);
draw_vertex(x2 + perp_x, y2 + perp_y);
draw_primitive_end();*/