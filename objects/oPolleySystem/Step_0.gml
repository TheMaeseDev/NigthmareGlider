
// Si el sistema tiene activado el retorno a reposo
/*if (returnToRest && !platA.canMove && !platB.canMove) {
	// Mover ambas plataformas hacia el nivel de reposo
	if (platA.y != restY) platA.y = lerp(platA.y, restY, 0.01);
	if (platB.y != restY) platB.y = lerp(platB.y, restY, 0.01);
}*/
